# Проект: Поиск вакансий с hh.ru (анализ данных в PostgreSQL)

## Описание проекта

Данный проект представляет собой консольное приложение на Python, которое:
- Получает данные о работодателях и их вакансиях с публичного API hh.ru.
- Сохраняет полученные данные в базу данных PostgreSQL.
- Предоставляет пользователю интерактивный интерфейс для аналитики: список компаний
с количеством вакансий, все вакансии, средняя зарплата, вакансии с зарплатой выше
средней, поиск по ключевым словам.

## Технологии

- Python 3.13
- PostgreSQL (база данных)
- psycopg2 — драйвер для работы с PostgreSQL
- requests — для запросов к API hh.ru
- python-dotenv — для управления переменными окружения
- mypy, flake8, isort, black — статическая типизация и линтинг

## Структура проекта

```

Project4_Course_work3_Skypro/
├── src/
│   ├── init.py
│   ├── api_client.py      # Взаимодействие с API hh.ru
│   ├── config.py          # Загрузка конфигурации из .env
│   ├── db_manager.py      # Класс DBManager (запись и чтение БД)
│   └── utils.py           # Создание БД и таблиц
├── .env                   # Переменные окружения (не попадает в репозиторий)
├── .env.example           # Пример файла .env
├── .flake8                # Конфигурационный файл 
├── .gitignore             # Игнорируемые файлы
├── main.py                # Точка входа, пользовательское меню
├── pyproject.toml         # Зависимости проекта
└── README.md              # Описание проекта

```
## Документация классов:

### `class HHAPIClient`
Класс для работы с публичным API hh.ru.

**Основные методы:**

1. get_employer(self, employer_id: int) -> Dict[str, Any]:
        """Получить информацию о работодателе по ID."""
2. get_vacancies(self, employer_id: int, per_page: int = 100) -> List[Dict[str, Any]]:
        """Получить список вакансий для указанного работодателя."""


### `class DBManager`
Класс для работы с БД PostgreSQL. Отвечает за вставку данных о работодателях и вакансиях,
а также за выполнение аналитических запросов к базе.

**Основные методы:**

1. insert_employer(self, employer_data: Dict[str, Any]) -> None:
        """Добавляет информацию о работодателе в таблицу employers ('id', 'name', 'url', 'description').
        Если работодатель с таким employer_id уже существует, запись игнорируется."""
2. insert_vacancy(self, vacancy_data: Dict[str, Any], employer_id: int) -> None:
        """Добавляет информацию о вакансии в таблицу vacancies.
        Если вакансия с таким vacancy_id уже существует, запись игнорируется.
        Поле salary в ответе API может отсутствовать, поэтому обрабатываем его аккуратно."""
3. get_companies_and_vacancies_count(self) -> List[Tuple[str, int]]:
       """Получает список всех компаний и количество вакансий, опубликованных каждой
       компанией. Возвращает список кортежей (название_компании, количество_вакансий)."""
4. get_all_vacancies(self) -> List[Tuple[str, str, Optional[int], Optional[int], Optional[str], str]]:
        """Получает список всех вакансий с подробной информацией:
        название компании, название вакансии, зарплата (от, до), валюта, ссылка на вакансию.
        Возвращает список кортежей."""
5. get_avg_salary(self) -> float:
        """Вычисляет среднюю зарплату по всем вакансиям.
        Для каждой вакансии берётся среднее арифметическое между salary_from и salary_to,
        затем вычисляется общее среднее.
        Вакансии, у которых не указана ни одна граница зарплаты, игнорируются.
        Возвращает среднюю зарплату (float). Если подходящих вакансий нет, возвращается 0.0."""
6. get_vacancies_with_higher_salary(self,) -> List[Tuple[str, str, Optional[int], Optional[int], Optional[str], str]]:
        """Возвращает список вакансий, у которых средняя зарплата (от+до)/2 выше средней зарплаты
        по всем вакансиям. Возвращает список вакансий (аналогично get_all_vacancies), отфильтрованный
        по условию."""
7. get_vacancies_with_keyword(self, keyword: str) -> List[Tuple[str, str, Optional[int], Optional[int], Optional[str], str]]:
        """Возвращает список вакансий, в названии которых содержится заданное ключевое слово, например, "python"
        (без учёта регистра). Возвращает список вакансий, удовлетворяющих условию."""

# Использование классов:

from src.api_client import HHAPIClient
from src.db_manager import DBManager

## Функциональность:

1. def main() -> None:
    """Главная функция приложения. Последовательно выполняет все шаги проекта:
    - подготовка БД,
    - наполнение данными (только при первом запуске),
    - диалог с пользователем."""
2. def create_database() -> None:
    """Создаёт базу данных PostgreSQL, если она не существует. Подключается к стандартной системной
    базе данных 'postgres', проверяет наличие целевой БД и создаёт её при отсутствии."""
3. def create_tables() -> None:
    """Создаёт таблицы employers и vacancies в целевой базе данных.
    Если таблицы уже существуют, ничего не пересоздаётся (благодаря IF NOT EXISTS).
    Устанавливается связь внешнего ключа между вакансиями и работодателями."""

# Использование функций:

from src.utils import create_database
from src.utils import create_tables

# Схема базы данных 

```
sql
employers (
    employer_id INT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    url VARCHAR(255),
    description TEXT
);

vacancies (
    vacancy_id INT PRIMARY KEY,
    employer_id INT REFERENCES employers(employer_id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    salary_from INT,
    salary_to INT,
    currency VARCHAR(3),
    url VARCHAR(255)
);
```

## Установка и запуск

### 1. Клонируйте репозиторий

```
git clone https://github.com/Margarita2405/Project4_Course_work3_Skypro
cd Project4_Course_work3_Skypro
```
### 2. Создайте и активируйте виртуальное окружение

```
python -m venv venv
source venv/bin/activate   # Linux/Mac
venv\Scripts\activate      # Windows
```

### 3. Установите зависимости

```
pip install -r requirements.txt
```

### 4. Настройте подключение к PosgreSQL

Создайте файл .env в корне проекта (или скопируйте из .env.example):

```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hh_parser
DB_USER=postgres
DB_PASSWORD=ваш_пароль
```

### 5. Запустите приложение

```
python main.py
```

При первом запуске программа автоматически создаст базу данных и таблицы,
затем загрузит данные о 10 предустановленных компаниях и их вакансиях.
При последующих запусках загрузка выполняться не будет (если таблицы уже заполнены).

### 6. Использование

После запуска появляется интерактивное меню:

1. Список компаний и количество вакансий
2. Список всех вакансий
3. Средняя зарплата по всем вакансиям
4. Вакансии с зарплатой выше средней
5. Поиск вакансий по ключевому слову
0. Выход

Выберите пункт, введя соответствующую цифру. Для поиска по ключевому слову введите
слово (например, python). Результаты выводятся в читаемом формате.

### 7. Примеры работы

<details>
<summary>Список компаний и количество вакансий</summary>

```
Яндекс: 15 вакансий
Сбер: 8 вакансий
Тинькофф: 22 вакансии
...
```

<details>
<summary>Поиск по ключевому слову "python"</summary>

```
Яндекс | Senior Python Developer | ЗП: 250000 - 350000 RUB | https://hh.ru/vacancy/123456
Тинькофф | Python-разработчик (удаленно) | Зарплата не указана | https://hh.ru/vacancy/789012
```

# Тестирование

## Для запуска линтеров:

black src/utils.py
isort src/utils.py
flake8 main.py
mypy main.py

## Документация:

Для получения дополнительной информации обратитесь 
к [документации](README.md).

## Лицензия

Проект разработан в рамках курсовой работы.